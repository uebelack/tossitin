const extractCommitMessagePrompt = `You are a helpful assistant that extracts a concise and informative commit message from the given text into the given structure.`;
export default extractCommitMessagePrompt;
